---
layout: post
title: "工具与 LLM 的交互 — 分词器"
date: 2025-08-14 00:00:00 +0800
tags: [LLM, 分词器, Tokenizer, Ollama, llama.cpp]
category: 技术分享
---

# 工具与LLM的交互

## 原始文本 → token ID  ，具体是如何工作的？

"原始文本 → token ID" 就是把人类可读的字符串切成一串整数编号，让 Transformer 模型能直接做矩阵乘法。整个过程分 4 步，下面用 Ollama/llama.cpp 的实际代码路径举例说明。

1. 拿到原始文本

   Ollama 收到 HTTP 请求里的 `messages` 数组后，先按 ChatML 模板拼成一段纯文本：

   ```txt
   <|im_start|>system
   You are a helpful assistant<|im_end|>
   <|im_start|>user
   上海在哪里？<|im_end|>
   <|im_start|>assistant
   ```

   这一段就是"原始文本"。

2. 加载分词器（Tokenizer）
   - 每个 GGUF 模型文件里都带一个 `tokenizer.model`（SentencePiece）或 `tokenizer.json`（Hugging Face BPE）。
   - llama.cpp 启动时把分词器表读进内存，得到一个 `std::unordered_map<std::string, llama_vocab::id>`，键是子词字符串，值是 token ID。

3. 子词切分 + 查表
   以 SentencePiece 为例：

    ```cpp
    std::vector<llama_vocab::id> tokens;
    llama_tokenize(ctx, text.c_str(), tokens, /*add_bos=*/true);
    ```

   内部流程：

    a) 预归一化（NFKC、小写、去除多余空格）
    b) SentencePiece 的 BPE 算法把整句递归拆成最小可合并的子词

      ```txt
      "上海" → ["上海"]（在中文里常是整字）
      "在哪里？" → ["在", "哪里", "？"]
      ```

    c) 查表得到 ID

      ```txt
      "上海" → 26710
      "在"   → 319
      "哪里" → 13156
      "？"   → 364
      ```

    d) 在开头加 `<s>`（BOS）或 `<|im_start|>` 这类特殊 token，最终得到

      ```txt
      tokens = [1, 26710, 319, 13156, 364, 32001, ...]
      ```

4. 送进模型推理

  tokens 数组就是 `int32_t` 的一维向量，直接拷到 GPU 的输入张量 `input_ids`，后续就是标准的 Transformer 前向计算。

### 可视化一句话的完整过程

原始文本：`I love Shanghai`
→ 归一化：`I love Shanghai`
→ BPE 子词：`["I", "▁love", "▁Shanghai"]`
→ 查表：`[40, 1107, 29358]`
→ 加 BOS：`[1, 40, 1107, 29358]`   ← 这就是模型真正收到的 token ID 序列。

### 总结

llama.cpp 的分词器把文本拆成子词，再查哈希表得到整数 ID；Ollama 只负责把 HTTP 文本交给它，不做任何额外的"向量化"。
